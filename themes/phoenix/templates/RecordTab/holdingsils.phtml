<?php
    $recordTitle = $this->escapeHtml($this->driver->getShortTitle() . ' ' . $this->driver->getSubtitle() . ' ' . $this->driver->getTitleSection());
    $cleanTitle = trim($recordTitle, ' /.');
    // Set up convenience variables:
    $account = $this->auth()->getManager();
    $user = $account->isLoggedIn();
    $openUrl = $this->openUrl($this->driver, 'holdings');
    $openUrlActive = $openUrl->isActive();
    $doi = $this->doi($this->driver, 'holdings');
    $doiActive = $doi->isActive();
    $offlineMode = $this->ils()->getOfflineMode();
    $callNumDisplayText = '(Browse by Call Number)';
    $callNumLabel = 'Call Number:';
    // Account for replace_other_urls setting
    //$urls = $this->record($this->driver)->getLinkDetails($openUrlActive);
    $urls = $this->Eholdings()->getEholdingLinks();//Replace default 856 urls with e-holdings from the 928 fields (previously 098)
    $offlineMode = $this->ils()->getOfflineMode();
    try {
        $holdings = $this->driver->getRealTimeHoldings();
    } catch (\VuFind\Exception\ILS $e) {
        $holdings = ['holdings' => []];
        $offlineMode = 'ils-offline';
    }

    $bib = $this->driver->getUniqueID();
    $isSfxStub = strtolower(substr($bib, 0, 3)) == 'sfx';
    $sfxNum = $isSfxStub ? substr($bib, 4) : '';
    $issns = $this->driver->getISSNs();
    $hasIssns = count($issns) > 0;

    // E-resource links
    $hathiLink = $this->HathiLink()->getHathiLink();
    $hasFullTextOnline = $this->HathiLink()->hasFullTextOnline($holdings);
    $hasRelatedTextOnline = $this->HathiLink()->hasFullTextOnline($holdings, $related=true);

    // Whitelist for things without a barcode that need to display when a barcode isn't present.
    $itemStatusWhitelist = array('INPROCESS', 'ONORDER');

    // Set page title.
    $this->headTitle($this->translate('Holdings') . ': ' . $this->driver->getBreadcrumb());

    // Item locations that are off site storage
    $OFFSITE = ['btaaspr'];

    // Emergency closure set in the config
    $emergency_closure = $this->GetConfig()->getEmergencyClosure();
?>

<?=$this->context($this)->renderInContext('librarycards/selectcard.phtml', ['user' => $this->auth()->isLoggedIn()]); ?>

<?php if (!empty($holdings['blocks'])):?>
  <div id="account-block-msg" class="alert alert-danger">
    <?=$this->transEsc('account_block_options_missing', ['%%details%%' => implode('; ', $holdings['blocks'])]) ?>
  </div>
<?php endif; ?>

<?=($offlineMode == "ils-offline") ? $this->render('Helpers/ils-offline.phtml', ['offlineModeMsg' => 'ils_offline_holdings_message']) : ''?>
<?php if (($this->ils()->getHoldsMode() == 'driver' && !empty($holdings['holdings'])) || $this->ils()->getTitleHoldsMode() == 'driver'): ?>
  <?php if ($account->loginEnabled() && $offlineMode != 'ils-offline'): ?>
    <?php if (!$user): ?>
      <div class="alert alert-info">
        <a href="<?=$this->recordLink()->getTabUrl($this->driver, 'Holdings')?>?login=true&amp;catalogLogin=true" data-lightbox><?=$this->transEsc("Login")?></a> <?=$this->transEsc("hold_login")?>
      </div>
    <?php elseif (!$user->cat_username): ?>
      <div class="alert alert-info">
        <?=$this->translate("hold_profile_html", ['%%url%%' => $this->recordLink()->getTabUrl($this->driver, 'Holdings') . '?catalogLogin=true'])?>
      </div>
    <?php endif; ?>
  <?php endif; ?>
<?php endif; ?>
<?php $holdingTitleHold = $this->driver->tryMethod('getRealTimeTitleHold'); if (!empty($holdingTitleHold)): ?>
  <a class="placehold" data-lightbox title="<?=$this->transEsc('request_place_text')?>" href="<?=$this->recordLink()->getRequestUrl($holdingTitleHold)?>"><i class="fa fa-flag" aria-hidden="true"></i>&nbsp;<?=$this->transEsc('title_hold_place')?></a>
<?php endif; ?>
<?php $hasLinks = !empty($urls) || $openUrlActive || $doiActive || $hathiLink || $isSfxStub ?>
<?php if ($hasLinks || $hasIssns): ?>
  <div class="holdings-locpanel online">
  <div class="locpanel-heading online">
  <h2><?=$this->transEsc("Online")?></h2><br/>
  <?php if (!empty($urls)): ?>
      <?/*For now, only display a browse link if the first e-holding has a callnumber - Fix later*/?>
      <?php if (isset($urls[0]['callnumber'])):?>
        <h3><span><?=$this->transEsc($callNumLabel)?> </span><?=$urls[0]['callnumber']?></h3> <a href="<?= $this->url('home')?>alphabrowse/Home?source=lcc&amp;from=<?=$this->escapeHtml($urls[0]['callnumber'])?>"
           class="ablink"><?=$callNumDisplayText?></a><br/>
      <?endif;?>
  <?php endif; ?>
  </div><!--/.locpanel-heading online-->
  <div id="e-links" class="holdings-unit" data-sfx="<?=$sfxNum?>" data-issns="<?=implode(',', $issns)?>" data-server-side-links="<?=$hasLinks ? 'true' : '' ?>">
  <?php if (!empty($urls)): ?>
    <?php foreach ($urls as $current): ?>
      <?php $linkText = (!empty($current['text']) ? ': '.$current['text'] : ''); ?>
      <div class="local-eholding">
        <a href="<?=$this->escapeHtmlAttr($this->proxyUrl($current['url']))?>" class="eLink"><?=$this->escapeHtml($this->Eholdings()->getEholdingsLinkText($current['type']) . $linkText)?></a> <?=$current['desc']?><br/>
      </div>
    <?php endforeach; ?>
  <?php endif; ?>

  <?php if ($openUrlActive): ?><?=$openUrl->renderTemplate()?><?php endif; ?>
  <?php if ($doiActive): ?><?=$doi->renderTemplate()?><?php endif; ?>

  <?/*Build a Hathi link for records with a 903 field = to "Hathi" or records with a bib# beginning with ocm or ocn.*/?>
  <?php if ($hathiLink) : ?>
    <?=$hathiLink?>    
  <?php endif; ?>
  <?=$this->record($this->driver)->getPreviewLink()?>
  <?=$this->ServiceLinks()->altText($this->driver->getFormats(), $bib)?>
  </div><!--/#e-links-->
  </div><!--/.holdings-locpanel-->
<?php endif; ?>
<?php foreach ($holdings['holdings'] ?? [] as $holding): ?>
<?/*Convenience variable for e-resources*/?>
<?$isFullTextOnline = $holding['location'] == 'FullText'?>
<?$isRelatedTextOnline = $holding['location'] == 'Related'?>

<?//Bound-withs?>
<?php if (!empty($holding['items'][0]['boundwiths'])): ?>
  <h3>This item is bound with: </h3>
  <p><?=!empty($holding['items'][0]['boundwiths']['cn']) ? '<strong>' . $this->transEsc($callNumLabel) . ' </strong>' . $holding['items'][0]['boundwiths']['cn'] : ''; ?></p>
  <p>
  <?php foreach ($holding['items'][0]['boundwiths'] as $key): ?>
    <?php if (array_key_exists('title', $key)):?>
      <?="<a href='" . $key['url'] . "'>" . $key['title'] . "</a>"; ?>
    <?endif;?><br/>
  <?php endforeach; ?></p>
<?php endif; ?>
<?//End Bound-withs?>

<div class="holdings-locpanel">
<div class="locpanel-heading">
<h2>
  <?php // Get the raw location text. This contains the holdings id # and other such things ?>
  <?php // Break it into component parts and use it for different purposes ?>
  <?php $rawLocationText = $this->transEsc('location_' . $holding['location'], [], $holding['location']); ?>
  <?php $locationText = implode('-', explode('-', $rawLocationText, -1)); ?>
  <?php if (isset($holding['locationhref']) && $holding['locationhref']): ?>
    <a href="<?=$holding['locationhref']?>" target="_blank"><?=$locationText?></a>
  <?php else: ?>
    <?=$locationText?>
  <?php endif; ?>
</h2>

<?php /* Loop over textual (summary) holdings and separate them for later use */?>
<?php $otherTextualHoldings = []; ?>
<?php $unboundIssues = []; ?>
<?php $unboundSupplements = []; ?>
<?php $holdingsNotes = []; ?>
<?php if (isset($holding['textfields'])): ?>
    <?php foreach ($holding['textfields'] as $textFieldName => $textFields): ?>
      <?php $isUnboundIssues = $textFieldName == 'unbound issues'; ?>
      <?php $isUnboundSupplements = $textFieldName == 'unbound supplements'; ?>
      <?php $isHoldingsNotes = $textFieldName == 'holdings notes'; ?>
      <?php if ($isHoldingsNotes): ?>
        <?php $holdingsNotes['textfields'] = [$textFieldName => $textFields]; ?>
      <?php elseif ($isUnboundIssues): ?>
        <?php $unboundIssues['textfields'] = [$textFieldName => $textFields]; ?>
      <?php elseif ($isUnboundSupplements): ?>
        <?php $unboundSupplements['textfields'] = [$textFieldName => $textFields]; ?>
      <?php else: ?>
        <?php $otherTextualHoldings[$textFieldName] = ['textfields' => $textFields]; ?>
      <?php endif; ?>
    <?php endforeach; ?>
<?php endif; ?>

<?php /* Variables for checking the status of Library Has*/?>
<?php $flatLibHas = [] ?>
<?php $libHasCount = 0 ?>
<?php $emptyLibHas = true ?>
<?php if (isset($otherTextualHoldings['issues'])): ?>
  <?php $flatLibHas = call_user_func_array('array_merge', $otherTextualHoldings['issues']); ?>
  <?php $libHasCount = count($flatLibHas); ?>
  <?php $emptyLibHas = empty(trim($flatLibHas[0])); ?>
<?php endif; ?>
<?php $doesNotHaveLibraryhas = ($libHasCount <= 1 && $emptyLibHas); ?>

<?php /* Variables for checking the status of holdings notes*/?>
<?php $flatHoldingsNotes = [] ?>
<?php $holdingNotesCount = 0 ?>
<?php $emptyHoldingNotes = true ?>
<?php if (isset($holdingsNotes['textfields'])): ?>
  <?php $flatHoldingsNotes = call_user_func_array('array_merge', $holdingsNotes['textfields']); ?>
  <?php $holdingNotesCount = count($flatHoldingsNotes); ?>
  <?php $emptyHoldingNotes = empty(trim($flatHoldingsNotes[0])); ?>
<?php endif; ?>
<?php $noHoldingsNotes = ($holdingNotesCount <= 1 && $emptyHoldingNotes); ?>

<?/*Get the first barcode to pass to the map links*/ ?>
<?php $firstBarcode = $this->tab->getFirstBarcode($holding);?>

<?/*Callnumbers*/?>
<?php $lowerLoc = trim(strtolower($locationText)) ?>
<?php $isSpecial = $lowerLoc == 'on order' || $lowerLoc == 'received; in process'; ?>
<?php $callNos = $this->tab->getUniqueCallNumbersPhoenix($holding['items']); if (!empty($callNos) || $isSpecial): ?>
<?php $hasCallNos = !empty($callNos['display']); ?>
<?php if ($hasCallNos):?>
<?foreach ($callNos['display'] as $numKey => $callNo): ?>
   <?=$this->ServiceLinks()->maps(trim($locationText), $this->escapeHtml(trim($callNos['sort'][$numKey])), trim($callNos['prefix']))?>
   <br/><h3><span><?=$hasCallNos ? $this->transEsc($callNumLabel) . ' ' : ''?></span> <?=$this->escapeHtml($callNo)?></h3>
   <?php $source = ($callNos['typeid'][$numKey] == '3') ? 'dewey' : 'lcc'; ?>
   <a href="<?= $this->url('home')?>alphabrowse/Home?source=lcc&amp;from=<?=$this->escapeHtml($callNos['sort'][$numKey])?>"
      class="ablink"><?=$callNumDisplayText?></a><br/>
 <?php endforeach; ?>
<?php endif; ?>
</div><!--/.locpanel-heading-->

<div class="holdings-unit">
<?php if ($isFullTextOnline):?>
  <strong><?=$this->transEsc("Available via:")?></strong>
<?php endif; ?>
<?php if ($isRelatedTextOnline):?>
  <br/><strong><?=$this->transEsc("Related Links:")?></strong>
<?php endif; ?>

  <?php /* Use the stored and separated textual (summary) holdings */?>
  <?php if (isset($holding['textfields'])): ?>
      <?php if (!$noHoldingsNotes): ?>
        <p class="holdings-notes"><strong>Holdings notes: </strong> 
          <?php foreach ($holdingsNotes['textfields']['holdings notes'] as $holdingNote): ?>
            <?=$holdingNote?>
          <?php endforeach; ?>
        </p>
      <?php endif; ?>

      <?php if (!$doesNotHaveLibraryhas): ?>
        <?php /* The number of ownership types*/?>
        <?php $numTextHoldings = count($otherTextualHoldings); ?>
        <?php $summDivClass = ($numTextHoldings > 2 ? 'col-sm-4' : 'col-sm-6') ;?>
        <div class="section row libraryhas">
          <h5>Library has:</h5>
          <?php foreach ($otherTextualHoldings as $textFieldName => $textFieldArray): ?>
              <?php // Translation for summary is a special case for backwards-compatibility ?>
              <div class="summary-holdings <?=$summDivClass?>" data-display="<?=$this->tab->getDisplayNumber()?>">
                <h6>
                    <?php if ($textFieldName == 'summary'): ?>
                        <?=$this->transEsc("Volume Holdings")?>
                    <?php elseif ($textFieldName == 'issues'): ?>
                        <?=$this->transEsc("Issues/Volumes")?>
                    <?php else: ?>
                        <?=$this->transEsc(ucfirst($textFieldName))?>
                    <?php endif; ?>
                </h6>
                <ul>
                <?php foreach ($textFieldArray['textfields'] as $current): ?>
                  <li typeof="Enumeration"><?=$this->escapeHtml($current)?></li>
                <?php endforeach; ?>
                </ul>
              </div>
          <?php endforeach;?>
        </div>
      <?php endif; ?>

      <?php /*Unbound issues and unbound supplements*/ ?>
      <?php $splitChar = '^'; ?>
      <?php /*Unbound Suplements*/?>
      <?php if ($unboundIssues): ?>
        <?php $unboundLocation = explode($splitChar, $unboundIssues['textfields']['unbound issues'][0])[1]; ?>
        <?php foreach ($unboundIssues['textfields'] as $textFieldName => $textFields): ?>
          <table class="holdings-table table unbound" data-display="<?=$this->tab->getDisplayNumber()?>">
            <thead>
              <tr>
                <th colspan="3">
                    <h5><?=$this->transEsc(ucfirst($textFieldName))?>:</h5>
                    <span class="unbound-location"><?=$unboundLocation?></span>
                </th>
              </tr>
            </thead>
            <tbody>
              <?php foreach ($textFields as $current): ?>
                <?php $arr = explode($splitChar, $current); ?>
                <?php if (!empty(trim($arr[0]))): ?>
                  <tr vocab="http://schema.org/" typeof="Offer">
                    <th><?=$this->escapeHtml($arr[0])?></th>
                    <td>
                      Check shelf for availability
                      <link property="availability" href="http://schema.org/InStock"/>
                      <span class="loan-period">Building use only</span>
                    </td>
                  </tr>
                <?php endif; ?>
              <?php endforeach;?>
            </tbody>
          </table>
        <?php endforeach;?>
      <?php endif; ?>

      <?php /*Unbound supplements*/ ?>
      <?php if ($unboundSupplements): ?>
        <?php $unboundLocation = explode($splitChar, $unboundSupplements['textfields']['unbound supplements'][0])[1]; ?>
        <?php foreach ($unboundSupplements['textfields'] as $textFieldName => $textFields): ?>
          <table class="holdings-table table unbound" data-display="<?=$this->tab->getDisplayNumber()?>">
            <thead>
              <tr>
                <th colspan="3">
                    <h5><?=$this->transEsc(ucfirst($textFieldName))?>:</h5>
                    <span class="unbound-location"><?=$unboundLocation?></span>
                </th>
              </tr>
            </thead>
            <tbody>
              <?php foreach ($textFields as $current): ?>
                <?php $arr = explode($splitChar, $current); ?>
                <?php if (!empty(trim($arr[0]))): ?>
                  <tr vocab="http://schema.org/" typeof="Offer">
                    <th><?=$this->escapeHtml($arr[0])?></th>
                    <td>
                      Check shelf for availability
                      <link property="availability" href="http://schema.org/InStock"/>
                      <span class="loan-period">Building use only</span>
                    </td>
                  </tr>
                <?php endif; ?>
              <?php endforeach;?>
            </tbody>
          </table>
        <?php endforeach;?>
      <?php endif; ?>

  <?php endif; ?>

<?php // Create a boolean to see if holding has items ?>
<?php $hasItems = false; ?>
<?php foreach ($holding['items'] as $item): ?>
  <?php if (array_key_exists('holdtype', $item)): ?>
    <?php $hasItems = true; break; ?>
  <?php endif; ?>
<?php endforeach; ?>

<?php // Only display availability heading if holding has items ?>
<?php if ($hasItems): ?>
    <h5>Availability:</h5>
    <?php if($unboundIssues || !$doesNotHaveLibraryhas): ?>
      <span class="holding-location"><?=$locationText?></span>
    <?php endif; ?>
<?php endif; ?>

<table class="holdings-table table" data-display="<?=$this->tab->getDisplayNumber()?>">
  <caption><?=$this->transEsc('Holdings details from')?> <?=$this->transEsc($locationText)?></caption>
  <!--<tr>
    <th><?//=$hasCallNos ? $this->transEsc($callNumLabel) . ' ' : ''?></th>
    <td>
      <?//foreach ($callNos['display'] as $numKey => $callNo): ?>
        <?//=$this->escapeHtml($callNo)?>
        <a href="<?//= $this->url('home')?>alphabrowse/Home?source=lcc&from=<?//=$this->escapeHtml($callNos['sort'][$numKey])?>"
           class="ablink"><?//=$callNumDisplayText?></a><br/>
      <?php //endforeach; ?>
    </td>
  </tr>-->
  <?php endif; ?>
  <?php foreach ($holding['items'] as $row): ?>
    <?/*E-holding links if they exist*/?>
    <?php if (isset($row['eHolding']['uri'])):?>
        <br/><a href="<?=$this->proxyUrl($row['eHolding']['uri'])?>" class="fulltext eLink"><?=$this->transEsc("Full text online")?></a>
    <?php endif; ?>
    <?
      // AJAX Check record?
      $check = isset($row['check']) && $row['check'];
      $checkStorageRetrievalRequest = isset($row['checkStorageRetrievalRequest']) && $row['checkStorageRetrievalRequest'];
      $checkILLRequest = isset($row['checkILLRequest']) && $row['checkILLRequest'];
    ?>
    <?php if (isset($row['barcode']) && $row['barcode'] != "" || (in_array($row['status'], $itemStatusWhitelist))): ?>
      <tr vocab="http://schema.org/" typeof="Offer">
        <?php if ($row['number']):?>
        <th><?=$this->escapeHtml($row['number'])?></th>
        <?php endif; ?>
        <td>
          <?php if ($row['reserve'] == "Y"): ?>
            <link property="availability" href="http://schema.org/InStoreOnly" />
            <?=$this->transEsc("On Reserve - Ask at Circulation Desk")?><br />
          <?php endif; ?>
          <?php if (isset($row['use_unknown_message']) && $row['use_unknown_message']): ?>
            <span class="text-muted"><?=$this->transEsc("status_unknown_message")?></span>
          <?php else: ?>
            <?php if ($row['availability']): ?>
              <?php /* Begin Available Items (Holds) */ ?>
               <?php if ($emergency_closure): ?>
                 <?php $display_status = $this->transEsc($emergency_closure); ?>
                 <span class="<?=$this->GetConfig()->getECTextColor()?>">
               <?php else: ?>
                 <?php $display_status = $this->transEsc($row['status'] == 'AVAILABLE-AT-MANSUETO' ? 'Available' : $row['status_name'])?>
                 <span class="text-success">
               <?php endif; ?>
                   <?=$display_status?><link property="availability" href="http://schema.org/InStock" />
                   <?php if (isset($row['itemTypeCode']) && !empty($row['itemTypeName'])):?>
                      <span class="loan-period">Loan period: <?=$row['itemTypeName']?>&nbsp;</span>
                   <?endif;?>
               </span>
               <span class="service-links">
               <?=$this->ServiceLinks()->asr($row)?>
               <?=$this->ServiceLinks()->paging($row)?>
               <?=$this->ServiceLinks()->inProcessAtMansueto($row)?>
               <?=$this->ServiceLinks()->debugging($row)?>
              <?php if (isset($row['link']) && $row['link']): ?>
                <!--<a class="<?=$check ? 'checkRequest ' : ''?>placehold" data-lightbox href="<?=$this->recordLink()->getRequestUrl($row['link'])?>"><i class="fa fa-flag" aria-hidden="true"></i>&nbsp;<?=$this->transEsc($check ? "Check Hold" : "Place a Hold")?></a>-->
              <?php endif; ?>
              <?php if (isset($row['storageRetrievalRequestLink']) && $row['storageRetrievalRequestLink']): ?>
                <a class="<?=$checkStorageRetrievalRequest ? 'checkStorageRetrievalRequest ' : ''?> placeStorageRetrievalRequest" data-lightbox href="<?=$this->recordLink()->getRequestUrl($row['storageRetrievalRequestLink'])?>"><i class="fa fa-flag" aria-hidden="true"></i>&nbsp;<?=$this->transEsc($checkStorageRetrievalRequest ? "storage_retrieval_request_check_text" : "storage_retrieval_request_place_text")?></a>
              <?php endif; ?>
              <?php if (isset($row['ILLRequestLink']) && $row['ILLRequestLink']): ?>
                <a class="<?=$checkILLRequest ? 'checkILLRequest ' : ''?>placeILLRequest" data-lightbox href="<?=$this->recordLink()->getRequestUrl($row['ILLRequestLink'])?>"><i class="fa fa-flag" aria-hidden="true"></i>&nbsp;<?=$this->transEsc($checkILLRequest ? "ill_request_check_text" : "ill_request_place_text")?></a>
              <?php endif; ?>
            <?php else: ?>
              <?php /* Begin Unavailable Items (Recalls) */ ?>
              <?php if ($row['status'] == 'ANAL'): ?>
                 To check availability
                 <?php $callNumBreak = preg_split("/\s[a-zA-Z0-9]+\./", $callNos['sort'][$numKey]); ?>
                 <?php if ($callNumBreak): ?>
                   <?php $c = $callNumBreak[0]; ?>
                 <?php else: ?>
                   <?php $c = $callNos['sort'][$numKey]; ?>
                 <?php endif; ?>
                 <?php // hAcK, handle bad data ?>
                 <?php if (isset($c)): ?>
                   <?php $c = explode(' ', $c)[0]; ?>
                 <?php endif; ?>
                 <a href="/vufind/alphabrowse/home?from=<?=$c;?>&source=lcc&consult=true">consult the series record</a>.
              <?php else: ?>
              <?php $statusClass = in_array(strtolower($row['status']), $OFFSITE) ? 'text-unknown' : 'text-danger' ?>
              <?php if(isset($row['status_name']) || $row['status'] == 'MISSING-FROM-MANSUETO'): ?>
              <span class=<?=$statusClass?>><?=$this->transEsc($row['status'] == 'MISSING-FROM-MANSUETO' ? 'Missing' : $row['status_name'])?><link property="availability" href="http://schema.org/OutOfStock" /></span>
              <?php endif; ?>
              <?php if (isset($row['returnDate']) && $row['returnDate']): ?>&ndash; <span class="small"><?=$this->escapeHtml($row['returnDate'])?></span><?php endif; ?>
              <?php if (isset($row['duedate']) && $row['duedate']): ?>
                <?/*Only display duedates for loaned items*/?>
                <?php $duedate = ($row['status'] == 'LOANED' ? $this->tab->getDueDate($row) : '');?>
                <?php if ($duedate == 'no due date unless recalled'): ?>
                <?php $dueHtml = '&ndash; <span class="small">no due date unless recalled</span>'; ?>
                <?php else: ?>
                <?php $dueHtml = (!empty($duedate) ? '&ndash; <span class="small">' . $this->transEsc("Due") . ': ' . $this->escapeHtml($duedate) . '</span>' : '');?>
                <?php endif; ?>
                <?=$dueHtml?>
              <?php endif; ?>
              <?php if (isset($row['requests_placed']) && $row['requests_placed'] > 0): ?>
                <span><?=$this->transEsc("Requests")?>: <?=$this->escapeHtml($row['requests_placed'])?></span>
              <?php endif; ?>
              <?php endif; //end if/else for row['status'] == 'ANAL'?>
              <span class="service-links">
              <?=$this->ServiceLinks()->asr($row)?>
              <?=$this->ServiceLinks()->getIt($row)?>
              <?=$this->ServiceLinks()->debugging($row)?>
              <?php if (isset($row['link']) && $row['link'] && !$row['claimsReturned']): ?>
                <!--<a class="<?//=$check ? 'checkRequest' : ''?> modal-link inlineblock placehold" href="<?//=$this->recordLink()->getRequestUrl($row['link'])?>"><i class="fa fa-flag"></i>&nbsp;<?//=$this->transEsc($check ? "Check Recall" : "Recall This")?></a>-->
              <?php endif; ?>
            <?php endif; ?>
              <?php /* Begin Items that are either Available or Unavailable */ ?>
                <?=$this->ServiceLinks()->aeon($row)?>
                <?=$this->ServiceLinks()->altText($this->driver->getFormats(), $bib, $row)?>
                <?=$this->ServiceLinks()->scanAndDeliver($row)?>
                <?=$this->ServiceLinks()->offsite($row)?>
                <?=$this->ServiceLinks()->requestHelp($row, $cleanTitle)?>
                <?=$this->ServiceLinks()->requestHelpSCRC($row)?>
                </span><?php /* END .service-links */?>

                <?php /* Requests Unavailable Link */ ?>
                <?php $unavailableLink = $this->ServiceLinks()->requestsUnavailable($row)?>
                <?php if ($unavailableLink): ?>
                  <span class="service-links">
                    <?=$unavailableLink?>
                  </span>
                <?php endif; ?>

            <?php if (isset($row['itemNotes'])): ?>
              <div class="item-notes">
                <?=$row['itemNotes']?>
              </div>
            <?php endif; ?>
            <?php if (isset($row['itemCallnumberDisplay'])): ?>
              <span class="location-change">
                <i class="fa fa-arrow-circle-o-right" aria-hidden="true"></i>
                <strong>Shelved as:</strong>
                <?=$row['itemCallnumberDisplay']?>
              </span>
            <?php endif; ?>
            <?php if (isset($row['itemLocation'])): ?>
              <span class="location-change">
                <i class="fa fa-arrow-circle-o-right" aria-hidden="true"></i>
                <strong>Shelved in:</strong>
                <?=$row['itemLocation']?>
              </span>
            <?php endif; ?>
          <?php endif; ?>
          <?php /* Embed item structured data: library, barcode, call number */ ?>
          <?php if ($row['location']): ?>
            <meta property="seller" content="<?=$this->escapeHtmlAttr($row['location'])?>" />
          <?php endif; ?>
          <?php if ($row['barcode']): ?>
            <meta property="serialNumber" content="<?=$this->escapeHtmlAttr($row['barcode'])?>" />
          <?php endif; ?>
          <?php if ($row['callnumber']): ?>
            <meta property="sku" content="<?=$this->escapeHtmlAttr($row['callnumber'])?>" />
          <?php endif; ?>
          <?php /* Declare that the item is to be borrowed, not for sale */ ?>
            <link property="businessFunction" href="http://purl.org/goodrelations/v1#LeaseOut" />
            <link property="itemOffered" href="#record" />
        </td>
        <td width="2"></td>
      </tr>
    <?php endif; ?>
  <?php endforeach; ?>
  <?php if (!empty($holding['purchase_history'])): ?>
    <tr>
      <th><?=$this->transEsc("Most Recent Received Issues")?>:</th>
      <td>
        <?php foreach ($holding['purchase_history'] as $current): ?>
          <?=$this->escapeHtml($current['issue'])?><br/>
        <?php endforeach; ?>
      </td>
    </tr>
  <?php endif; ?>
</table>
</div><!--/.holdings-unit-->
</div><!--/.holdings-locpanel-->
<?php endforeach; ?>

<?php $history = $this->driver->getRealTimeHistory(); ?>
<?php if (is_array($history) && !empty($history)): ?>
<h3><?=$this->transEsc("Most Recent Received Issues")?></h3>
<table class="table table-striped">
  <?php foreach ($history as $row): ?>
    <tr><td><?=$this->escapeHtml($row['issue'])?></td></tr>
  <?php endforeach; ?>
</table>
<?php endif; ?>
